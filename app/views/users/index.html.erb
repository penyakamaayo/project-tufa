<div class="container">
  <div class="col-md-12">
    <%if current_user.admin?%>
      <bebas><h2>Admin's Dashboard</h2></bebas>
      <div class="users-index">
        <div class="row">
          <div class="col-md-12">
            <%= render 'index' %>
          </div>
        </div>
      </div>
      <% else %>
      <bebas><h2>User Dashboard</h2></bebas>
      <% @user = current_user %>
      <%= render 'show'%>
    <%end%>
  </div>
</div>
